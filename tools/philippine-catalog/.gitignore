# 3,983 fetched HTML pages. Regenerated by `python harvest.py fetch`, which is polite and
# cached, but takes ~100 minutes from cold. corpus.jsonl is committed so that is not needed.
cache/

# ~100 MB of Canadian Nutrient File CSVs. Open Government Licence - Canada, freely
# downloadable; see cnf.py for the URL and for why the update pack must NOT be applied.
cnf/

# Regenerated in seconds from corpus.jsonl by parse.py / phase2.py / compute.py.
parsed.jsonl
dishes.json
dishes.computed.json
mapping.candidates.tsv

# Run logs.
*.log

# Python bytecode.
__pycache__/
*.pyc
